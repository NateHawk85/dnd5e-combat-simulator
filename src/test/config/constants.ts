import SimulationConfiguration from "../../main/classes/SimulationConfiguration";

export const DEFAULT_DATA_VERSION = 0;

export const DEFAULT_SIMULATION_CONFIGURATION = new SimulationConfiguration();
export const SECONDARY_SIMULATION_CONFIGURATION = new SimulationConfiguration();