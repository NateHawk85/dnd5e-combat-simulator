export default class CreateSimulationConfigurationForm extends FormApplication {

    // TODO - test
    protected async _updateObject(event: Event, formData: object | undefined): Promise<unknown> {
        return Promise.resolve(undefined);
    }
}